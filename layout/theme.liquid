<!doctype html>
<html lang="en">
  <head>
    {{ content_for_header }} {# Shopify injects CSS/JS/Liquid #}
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ page_title }}</title>
  </head>
  <body>
    {{ content_for_layout }}
  </body>
</html>
